
# OpusPulse

OpsPulse is a mini data engineering project that fetches weather data from the Open-Meteo API, stores the raw response as JSON, transforms it into a structured format using Pandas, and saves the cleaned data as CSV. The pipeline also includes basic pytest tests to validate data structure and correctness.
